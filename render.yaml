services:
  - type: web
    name: student-management-system
    env: docker
    plan: free
    autoDeploy: true
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: deploy
      - key: JWT_SECRET
        generateValue: true
